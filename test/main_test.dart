import 'package:flutter_test/flutter_test.dart';
import 'package:mindease_app/main.dart' as app;

void main() {
  test('App can be created', () {
    expect(() => app.main(), returnsNormally);
  });
}
